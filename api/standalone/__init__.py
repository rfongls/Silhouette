"""Standalone Pipeline (isolated feature).

This package namespaces all UI/API for the legacy-compatible standalone pipeline.
It prefers any existing templates under templates/standalone/** and falls back to
vendored legacy panel templates under templates/standalone/legacy/** when needed.

Nothing in V2 (docs/v2, engine/**, or V2 UI) is modified by this package.
"""
